# Copy to .env.local for local dev. On Vercel: Project → Settings → Environment Variables (same names).

SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
RECEIVER_EMAIL=

# Optional — only if the site is deployed under a subpath (not needed for Vercel default domain)
# NEXT_PUBLIC_BASE_PATH=
